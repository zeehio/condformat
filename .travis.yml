language: r

r_binary_packages:
  - dplyr
  - lazyeval
  - scales
  - assertthat
  - knitr
  - testthat
  - htmlTable
  - rmarkdown

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr);codecov()'

sudo: required

