% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-pipe.R
\name{pipe}
\alias{pipe}
\alias{\%>\%}
\title{Pipe operator (soft-deprecated re-export)}
\usage{
lhs \%>\% rhs
}
\arguments{
\item{lhs}{A value, or the magrittr placeholder \code{.}}

\item{rhs}{A function call using magrittr semantics}
}
\value{
The result of piping \code{lhs} into \code{rhs}, per \link[magrittr:\%>\%]{magrittr::\%>\%}
}
\description{
condformat re-exports magrittr's \verb{\%>\%} for backward compatibility with existing code that relies on \code{library(condformat)} making it available (condformat's own code uses the native pipe, \verb{|>}, internally, since it requires R 4.1 or newer). Using it through condformat is deprecated: it emits a one-time-per-session warning, then behaves exactly like \link[magrittr:\%>\%]{magrittr::\%>\%}. Please import \verb{\%>\%} from magrittr or dplyr yourself, or switch to \verb{|>}, since this re-export will be removed in a future release.
}
